export enum SchemaValidationError {
  INVALID_MINIMAL_LENGTH = "This field is required",
  INVALID_EMAIL = "A valid email address is required",
  INVALID_PASSWORD = "Password must be at least 6 characters long",
}
